<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/mi_add_pedido"
        android:icon="@drawable/ic_add"
        android:onClick="adicionarPedido"
        android:title="Adicionar"
        app:showAsAction="always" />
    <item  android:id="@+id/mi_remover_pedido"
        android:title="Excluir"
        android:onClick="removerPedido"
        app:showAsAction="always"
        android:icon="@drawable/ic_remover"
        />
    <item  android:id="@+id/mi_editar_pedido"
        android:title="Editar"
        android:onClick="editarPedido"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_editar"
        />

</menu>